How to build :

Install all the dependencies : libvlc, Qt4, boost
You need SCons (a build system).

Under Debian based distributions, here is a list of packages providing these dependencies :
- libvlc-dev
- libqt4-dev
- libboost-filesystem-dev
- libboost-program-options-dev
- libboost-thread-dev
- libboost-serialization-dev

The build system builds 7 different programs :
- nnat
- mkcorpus
- featdraw
- specdraw
- classify
- live
- fingerprint

Compile all the programs :

   scons

Compile a specific program :

   scons specdraw

Clean :

   scons -c


